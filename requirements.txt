# CPU-only torch by default (dev machines, CI). On the lab's AMD MI300X,
# instead install the ROCm build per pytorch.org/get-started/locally before
# installing the rest of this file, or torch here will overwrite it.
--extra-index-url https://download.pytorch.org/whl/cpu
pandas>=2.0
pyarrow>=14.0
numpy>=1.24
scikit-learn>=1.6
xgboost>=2.0
scipy>=1.11
matplotlib>=3.7
seaborn>=0.12
mapie>=1.0
pyyaml>=6.0
jupyter>=1.0
torch>=2.2
